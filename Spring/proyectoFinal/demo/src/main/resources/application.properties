# =========================
# Configuración general
# =========================
spring.application.name=demo
server.port=${PORT:8080}
# =========================
# Base de datos PostgreSQL (Render)
# =========================
# Puedes usar variables de entorno para no poner la contraseña en el archivo:
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d6dh2795pdvs73fhikeg-a:5432/cs_skin_market_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:cs_skin_market_db_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:hoADYuLtv1P3EVRT0rIWrBKzxJQdQW7C}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# =========================
# H2 Console (opcional, solo local)
# =========================
spring.h2.console.enabled=true

# =========================
# Correo electrónico
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nevalenov.iaroslav@gmail.com
spring.mail.password=hecvlduqubpapund
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =========================
# Internacionalización
# =========================
spring.messages.basename=messages
spring.messages.encoding=UTF-8
spring.messages.fallback-to-system-locale=false