# =========================
# Configuración general
# =========================
spring.application.name=demo
server.port=${PORT:8080}

# =========================
# Base de datos MySQL (Aiven) - ACTUALIZADO
# =========================
# Cambiamos postgresql por mysql y añadimos los parámetros de SSL de Aiven
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=true&trustServerCertificate=true
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA - ACTUALIZADO para MySQL
# Cambiamos ddl-auto a 'update' para que cree tus tablas en Aiven automáticamente
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# =========================
# Correo y otros (Se mantienen igual)
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nevalenov.iaroslav@gmail.com
spring.mail.password=hecvlduqubpapund
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
brevo.api.key=xsmtpsib-584edcb6e00be8ddee893e4c6dce793ff8553631180ee22c081f711dd26a7397-J1RW5HBnIbTXSdWX

spring.messages.basename=messages
spring.messages.encoding=UTF-8